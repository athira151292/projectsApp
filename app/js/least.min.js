! function(e) {

    e.fn.least = function(l) {
        var t = e.extend({
            random: !0,
            scrollToGallery: !0,
            HiDPI: !1
        }, l);
        return this.each(function() {
            function l(l, t, a) {
                var i = e('<figure class="close"></figure>'),
                    s = e('<img src="' + t + '"/>'),
                    r = e("li a");

                s.load(function() {
                    console.log(t);
                    l.html(a.length ? "<section></section>" : ""), l.prepend(s).append(i).slideDown("slow"), r.removeClass("load");
                    getContent(t);
                }), i.on("click", function() {
                    e(".least-preview").slideToggle("slow"), r.removeClass("active")
                })
               
            }
            if (e(this).find("li a").click(function(t) {
                    var a = e(this),
                        i = a.attr("href"),
                        s = e(".least-preview"),
                        r = s.children("img"),
                        n = a.attr("data-caption") || "";
                    return r.length && i === r.attr("src") ? (s.slideToggle("slow"), void a.toggleClass("active")) : (r.length ? (a.addClass("active"), e(".least-gallery li a.active").removeClass("active"), s.slideUp("slow", function() {
                        l(s, i, n)
                    })) : l(s, i, n), void a.addClass("load active"))
                }), t.random && e(".least-gallery").each(function() {
                    var l = e(this),
                        t = l.children("li");
                    t.sort(function() {
                        var e = parseInt(8 * Math.random(), null),
                            l = e % 4,
                            t = e > 2 ? 1 : -1;
                        return l * t
                    }).appendTo(l)
                }), t.scrollToGallery && e(this).find("li a").click(function(l) {
                    l.preventDefault(), e("html, body").animate({
                        scrollTop: 0
                    }, 500)
                }), t.HiDPI && window.devicePixelRatio > 1)
                for (var a = e("#least img"), i = e("#least a"), s = 0; s < a.length && i.length; s++) {
                    var r = a[s].src,
                        n = i[s].href,
                        o = r.lastIndexOf("."),
                        c = n.lastIndexOf(".");
                    r = r.substr(0, o) + "@2x" + r.substr(o), n = n.substr(0, c) + "@2x" + n.substr(c), a[s].src = r, i[s].href = n
                }
        });
    }
}(jQuery);
function getContent(imgurl){
    $.getJSON( "json/projects.json", function(data){
        var projects = data.projects;
        var arr=[];
        for(key in projects) {
            arr.push(key);
        }
        var length = arr.length;
        for(i=0;i<length;i++){
            if(projects[i].project.img == imgurl){
                var a = projects[i].project;
                $(".least-preview section").append('<table><tbody><tr><td>Name</td><td>'+a.name+'</td></tr><tr><td>Customer</td><td>'+a.customer+'</td></tr><tr><td>Category</td><td>'+a.category+'</td></tr></tr><tr><td>Status</td><td>'+a.status+'</td></tr></tr><tr><td>Start Date</td><td>'+a.start_date+'</td></tr></tr><tr><td>End Date</td><td>'+a.end_date+'</td></tr></tr><tr><td>Technologies used</td><td>'+a.tech+'</td></tr></tr><tr><td>Description</td><td>'+a.desc+'</td></tr></tbody></table>');
            }
        }
    });
}
//# sourceMappingURL=./least.min.map